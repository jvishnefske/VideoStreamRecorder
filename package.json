{
  "name": "video-stream-recorder-frontend",
  "version": "1.0.0",
  "description": "Frontend for video stream recorder",
  "scripts": {
    "dev": "parcel frontend/src/index.html --dist-dir frontend/dist",
    "build": "parcel build frontend/src/index.html --dist-dir frontend/dist --public-url /",
    "clean": "rm -rf frontend/dist .parcel-cache"
  },
  "devDependencies": {
    "parcel": "^2.9.3",
    "@parcel/transformer-sass": "^2.9.3"
  },
  "dependencies": {
    "rxjs": "^7.8.1",
    "@swc/helpers": "^0.5.5"
  },
  "browserslist": [
    "last 2 versions"
  ]
}